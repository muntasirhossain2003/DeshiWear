# Copy to .env — leave MONGO_URI empty to use the embedded dev database
MONGO_URI=
PORT=9002
JWT_SECRET=change_me

# AI shopping assistant — both free tiers, no credit card required.
# Groq: https://console.groq.com/keys (primary — fastest)
# Gemini: https://aistudio.google.com/apikey (fallback if Groq errors/rate-limits)
GROQ_API_KEY=
GEMINI_API_KEY=
